apiVersion: admissionregistration.k8s.io/v1
kind: ValidatingAdmissionPolicy
metadata:
  name: readinessprobe-httpget-host-containers
  annotations:
    security.kubeapt.io/displayName: "Readinessprobe Httpget Host"
    security.kubeapt.io/description: "Redirecting the readiness HTTP probe to another host makes the kubelet fetch an arbitrary URL from the node, reaching metadata and node-local endpoints closed to pods, while Service traffic keeps reaching an unchecked container."
    security.kubeapt.io/resource: "Containers"
    security.kubeapt.io/severity: "Low"
    security.kubeapt.io/remediation: "Remove the host field so the probe targets the pod IP, or set it to an empty string. Field: spec.containers[*].readinessProbe.httpGet.host."
    security.kubeapt.io/product: "Kubernetes"
spec:
  failurePolicy: Fail
  matchConstraints:
    resourceRules:
    - apiGroups:
      - ''
      apiVersions:
      - v1
      operations:
      - CREATE
      - UPDATE
      resources:
      - pods
  validations:
  - expression: |
      object.spec.containers.all(c,
        !has(c.readinessProbe) ||
        !has(c.readinessProbe.httpGet) ||
        !has(c.readinessProbe.httpGet.host) ||
        c.readinessProbe.httpGet.host == ""
      )
    message: |
      spec.containers[*].readinessProbe.httpGet.host must be undefined or empty ("").
