apiVersion: admissionregistration.k8s.io/v1
kind: ValidatingAdmissionPolicy
metadata:
  name: read-only-root-filesystem-initcontainers
  annotations:
    security.kubeapt.io/displayName: "Read Only Root Filesystem"
    security.kubeapt.io/description: "A writable init container root filesystem lets an intruder drop tooling, overwrite application binaries, and rewrite configuration in place, leaving a foothold in the running pod that nothing in the image would reveal."
    security.kubeapt.io/resource: "Initcontainers"
    security.kubeapt.io/severity: "Moderate"
    security.kubeapt.io/remediation: "Set readOnlyRootFilesystem to true on every init container and mount an emptyDir volume over each path the process genuinely needs to write. Field: spec.initContainers[*].securityContext.readOnlyRootFilesystem."
    security.kubeapt.io/product: "Kubernetes"
spec:
  failurePolicy: Fail
  matchConstraints:
    resourceRules:
    - apiGroups:
      - ''
      apiVersions:
      - v1
      operations:
      - CREATE
      - UPDATE
      resources:
      - pods
  validations:
  - expression: |
      (has(object.spec.os) &&
       has(object.spec.os.name) &&
       object.spec.os.name.lowerAscii() == "windows") ||
      !has(object.spec.initContainers) || object.spec.initContainers.all(ic,
        has(ic.securityContext) &&
        has(ic.securityContext.readOnlyRootFilesystem) &&
        ic.securityContext.readOnlyRootFilesystem == true
      )
    message: |
      spec.initContainers[*].securityContext.readOnlyRootFilesystem must be set to true.
