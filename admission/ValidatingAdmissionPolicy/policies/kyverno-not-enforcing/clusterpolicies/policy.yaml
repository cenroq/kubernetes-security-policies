apiVersion: admissionregistration.k8s.io/v1
kind: ValidatingAdmissionPolicy
metadata:
  name: kyverno-clusterpolicy-not-enforcing
  annotations:
    security.kubeapt.io/displayName: "Kyverno ClusterPolicy Not Enforcing"
    security.kubeapt.io/description: "A ClusterPolicy left on Audit only records violations in a report, so the workloads it was written to block are admitted cluster-wide anyway and the protection exists only as a log entry."
    security.kubeapt.io/resource: "ClusterPolicies"
    security.kubeapt.io/severity: "Moderate"
    security.kubeapt.io/remediation: "Set the policy-level validationFailureAction to Enforce and change any rule that overrides it with Audit. Fields: spec.validationFailureAction, spec.rules[*].validate.failureAction."
    security.kubeapt.io/product: "Kyverno"
spec:
  failurePolicy: Fail
  matchConstraints:
    resourceRules:
    - apiGroups:
      - kyverno.io
      apiVersions:
      - v1
      operations:
      - CREATE
      - UPDATE
      resources:
      - clusterpolicies
  validations:
  - expression: |
      !(object.spec.?validationFailureAction.orValue('Enforce') in ['Audit', 'audit']) &&
      !object.spec.?rules.orValue([]).exists(r,
        r.?validate.failureAction.orValue('Enforce') in ['Audit', 'audit']
      )
    message: |
      Kyverno ClusterPolicies must enforce (not Audit) validation failures.
