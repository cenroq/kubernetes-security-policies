apiVersion: admissionregistration.k8s.io/v1
kind: ValidatingAdmissionPolicy
metadata:
  name: kyverno-policy-not-enforcing
  annotations:
    security.kubeapt.io/displayName: "Kyverno Policy Not Enforcing"
    security.kubeapt.io/description: "A Policy left on Audit only records violations in a report, so the workloads it was written to block are still admitted into its namespace and the protection exists only as a log entry."
    security.kubeapt.io/resource: "Policies"
    security.kubeapt.io/severity: "Moderate"
    security.kubeapt.io/remediation: "Set the policy-level validationFailureAction to Enforce and change any rule that overrides it with Audit. Fields: spec.validationFailureAction, spec.rules[*].validate.failureAction."
    security.kubeapt.io/product: "Kyverno"
spec:
  failurePolicy: Fail
  matchConstraints:
    resourceRules:
    - apiGroups:
      - kyverno.io
      apiVersions:
      - v1
      operations:
      - CREATE
      - UPDATE
      resources:
      - policies
  validations:
  - expression: |
      !(object.spec.?validationFailureAction.orValue('Enforce') in ['Audit', 'audit']) &&
      !object.spec.?rules.orValue([]).exists(r,
        r.?validate.failureAction.orValue('Enforce') in ['Audit', 'audit']
      )
    message: |
      Kyverno Policies must enforce (not Audit) validation failures.
