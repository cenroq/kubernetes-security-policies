apiVersion: v1
kind: PersistentVolume
metadata:
  name: violating-persistentvolume-hostpath
spec:
  capacity:
    storage: 1Gi
  accessModes:
  - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  hostPath:
    path: /tmp/data
