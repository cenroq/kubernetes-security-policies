apiVersion: admissionregistration.k8s.io/v1
kind: ValidatingAdmissionPolicy
metadata:
  name: validatingwebhookconfiguration-hardening
  annotations:
    security.kubeapt.io/displayName: "Webhook Hardening"
    security.kubeapt.io/description: "ValidatingWebhookConfigurations must not use failurePolicy Ignore or an external clientConfig.url."
    security.kubeapt.io/resource: "ValidatingWebhookConfigurations"
    security.kubeapt.io/severity: "Moderate"
    security.kubeapt.io/remediation: "Set failurePolicy to Fail and back each webhook with an in-cluster service (clientConfig.service) instead of an external url. Fields: webhooks[*].failurePolicy, webhooks[*].clientConfig.url."
    security.kubeapt.io/product: "Kubernetes"
spec:
  failurePolicy: Fail
  matchConstraints:
    resourceRules:
    - apiGroups:
      - admissionregistration.k8s.io
      apiVersions:
      - v1
      operations:
      - CREATE
      - UPDATE
      resources:
      - validatingwebhookconfigurations
  validations:
  - expression: |
      !has(object.webhooks) || object.webhooks.all(w,
        w.?failurePolicy.orValue('Fail') != 'Ignore'
      )
    message: |
      ValidatingWebhookConfigurations must not set failurePolicy to Ignore.
  - expression: |
      !has(object.webhooks) || object.webhooks.all(w,
        !has(w.clientConfig.url)
      )
    message: |
      ValidatingWebhookConfigurations must not use an external clientConfig.url; back the webhook with an in-cluster service.
