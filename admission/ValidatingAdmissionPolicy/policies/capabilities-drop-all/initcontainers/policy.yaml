apiVersion: admissionregistration.k8s.io/v1
kind: ValidatingAdmissionPolicy
metadata:
  name: capabilities-drop-all-initcontainers
  annotations:
    security.kubeapt.io/displayName: "Capabilities Drop All"
    security.kubeapt.io/description: "An init container that never drops ALL keeps the default runtime capability set, so code execution inside it inherits DAC_OVERRIDE, SETUID and NET_RAW for bypassing file permissions, regaining root and spoofing traffic."
    security.kubeapt.io/resource: "Initcontainers"
    security.kubeapt.io/severity: "Moderate"
    security.kubeapt.io/remediation: "Add ALL to the dropped capability list and re-add only the specific capabilities the workload actually needs. Field: spec.initContainers[*].securityContext.capabilities.drop."
    security.kubeapt.io/product: "Kubernetes"
spec:
  failurePolicy: Fail
  matchConstraints:
    resourceRules:
    - apiGroups:
      - ''
      apiVersions:
      - v1
      operations:
      - CREATE
      - UPDATE
      resources:
      - pods
  validations:
  - expression: |
      (has(object.spec.os) &&
       has(object.spec.os.name) &&
       object.spec.os.name.lowerAscii() == "windows") ||
      !has(object.spec.initContainers) || object.spec.initContainers.all(ic,
        has(ic.securityContext) &&
        has(ic.securityContext.capabilities) &&
        has(ic.securityContext.capabilities.drop) &&
        ic.securityContext.capabilities.drop.exists(d, d == "ALL")
      )
    message: |
      spec.initContainers[*].securityContext.capabilities.drop must exist and include "ALL".
