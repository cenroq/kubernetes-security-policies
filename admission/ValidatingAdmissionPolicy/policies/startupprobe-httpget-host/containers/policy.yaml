apiVersion: admissionregistration.k8s.io/v1
kind: ValidatingAdmissionPolicy
metadata:
  name: startupprobe-httpget-host-containers
  annotations:
    security.kubeapt.io/displayName: "Startupprobe Httpget Host"
    security.kubeapt.io/description: "Redirecting the startup HTTP probe to another host makes the kubelet fetch an arbitrary URL from the node, reaching metadata and node-local endpoints closed to pods, and a hung container passes its startup gate."
    security.kubeapt.io/resource: "Containers"
    security.kubeapt.io/severity: "Low"
    security.kubeapt.io/remediation: "Remove the host field so the probe targets the pod IP, or set it to an empty string. Field: spec.containers[*].startupProbe.httpGet.host."
    security.kubeapt.io/product: "Kubernetes"
spec:
  failurePolicy: Fail
  matchConstraints:
    resourceRules:
    - apiGroups:
      - ''
      apiVersions:
      - v1
      operations:
      - CREATE
      - UPDATE
      resources:
      - pods
  validations:
  - expression: |
      object.spec.containers.all(c,
        !has(c.startupProbe) ||
        !has(c.startupProbe.httpGet) ||
        !has(c.startupProbe.httpGet.host) ||
        c.startupProbe.httpGet.host == ""
      )
    message: |
      spec.containers[*].startupProbe.httpGet.host must be undefined or empty ("").
