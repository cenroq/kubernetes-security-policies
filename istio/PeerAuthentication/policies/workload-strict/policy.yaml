apiVersion: security.istio.io/v1
kind: PeerAuthentication
metadata:
  name: app-strict
  namespace: default
  annotations:
    cenroq.io/displayName: "Workload mTLS Strict"
    cenroq.io/description: "Enforce STRICT mTLS for the payments workload."
spec:
  selector:
    matchLabels:
      app: payments
  mtls:
    mode: STRICT
