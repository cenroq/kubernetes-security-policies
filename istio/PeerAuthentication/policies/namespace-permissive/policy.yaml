apiVersion: security.istio.io/v1
kind: PeerAuthentication
metadata:
  name: default
  namespace: permissive-namespace
  annotations:
    cenroq.io/displayName: "Namespace mTLS Permissive"
    cenroq.io/description: "Allow both plaintext and mTLS traffic within the permissive namespace."
spec:
  mtls:
    mode: PERMISSIVE
